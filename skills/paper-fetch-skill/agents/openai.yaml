interface:
  display_name: "Paper Fetch Skill"
  short_description: "Fetch verified paper text or probe full-text availability"
  default_prompt: "Use $paper-fetch-skill to fetch and verify the full text of the papers I provide, or probe their full-text availability if I only request a check."
